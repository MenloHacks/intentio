import * as Ajv from 'ajv';
/**
 * Returns validator instance
 */
export declare const getValidator: (schema: any) => Ajv.ValidateFunction;
/**
 * Converts object values to lower case
 * @param ob
 */
export declare const valuesToLowerCase: (ob: any) => any;
